{
  "name": "www",
  "version": "1.1.0",
  "description": "frontend.miami site",
  "author": "Chris Scott",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "yarn nuxt generate && yarn push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "bootstrap": "^4.1.3",
    "bootstrap-vue": "^2.0.0-rc.11",
    "cross-env": "^5.2.0",
    "jsonp": "^0.2.1",
    "nuxt": "^2.3.4"
  },
  "devDependencies": {
    "node-sass": "^4.13.1",
    "push-dir": "^0.4.1",
    "sass-loader": "^7.1.0"
  }
}
